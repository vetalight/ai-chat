## Project : streamlit application with ai chat
### Steps to install
    1. python.exe -m pip install --upgrade pip
    2. python -m venv env
    3. env\Scripts\activate
    4. pip install -r requirements.tx
    5. pip freeze > requirements.txt

## Start Application
streamlit run app.py
test